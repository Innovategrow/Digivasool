# Backend URL the web app calls. Leave empty when the backend serves the web app
# (production build served by FastAPI) or when using the dev-server proxy.
VITE_API_BASE_URL=
# Dev server only: where /api is proxied to
VITE_DEV_BACKEND=http://localhost:8001
